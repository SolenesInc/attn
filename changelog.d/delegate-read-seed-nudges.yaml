kind: fixed
area: garden
change: >
  Agent guidance now tells agents to read attn update notifications with the
  suggested command so the update is acknowledged, while preserving the
  boundary that reading an update does not authorize acting on it or
  interrupting the user.
symptom: >
  Agents could treat an update notification as optional awareness, leave it
  unread, and receive the same notification again.
