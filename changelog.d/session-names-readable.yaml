kind: changed
area: sessions
change: Session names can be 48 characters everywhere, delegation names that look like ids or truncated folders get a generated title, and `attn session rename` lets an agent rename a session when its work drifts.
