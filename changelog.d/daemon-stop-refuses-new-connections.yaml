kind: internal
area: testing
change: An in-process daemon stops listening as soon as it stops accepting, so agent wrappers reporting their exit are refused instead of stalling wire-test restarts, and `attn daemon ensure` starts a replacement only after the old daemon releases its lock.
