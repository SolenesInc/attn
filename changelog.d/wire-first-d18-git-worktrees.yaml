kind: internal
area: testing
change: Git status, file diff, GitHub polling and worktree create, delete and sweep promises are now guarded by daemon wire tests instead of in-package tests of daemon internals.
