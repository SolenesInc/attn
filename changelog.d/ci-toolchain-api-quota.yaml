kind: internal
area: ci
change: Restore Go and Node.js from the Actions cache so CI jobs stop exhausting the repository's hourly GitHub API quota, and report the quota at the start and end of each run.
