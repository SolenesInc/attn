kind: changed
area: sessions
change: Session state reaches the app the moment the agent reports it, instead of up to a second later, and an idle daemon no longer wakes every second to re-check settled sessions.
