// Bundlers reach for this one unless they are told to build for production, so the SDK carries it rather than leaving a default flag to fail a build.

export { Fragment, jsxDEV } from "react/jsx-dev-runtime"
export type { JSX } from "react/jsx-dev-runtime"
