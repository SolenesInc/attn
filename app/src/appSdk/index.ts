// An entry file, not a mapping straight at the package: the import map needs a URL
// that does not change between builds. See vite.config.ts's appSdkChunks plugin.
export * from "@victorarias/attn-app"
